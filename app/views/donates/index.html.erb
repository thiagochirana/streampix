<%= form_with model: @donate do |form| %>
  <div class="form-group">
    <%= form.label :nickname %>
    <%= form.text_field :nickname , class: "form-control", placeholder: "Quem é você?"%>
  </div>

  <div class="form-group">
    <%= form.label :message, "Mensagem" %>
    <%= form.text_area :message , class: "form-control", placeholder: "Sua mensagem para stream"%>
  </div>

  <div class="form-group">
    <%= form.label :value, "Valor" %>
    <%= form.number_field :value , class: "form-control", placeholder: "R$ 1,00"%>
  </div>

  <%= form.submit "Donate", class: "btn btn-success"%>
<% end %>